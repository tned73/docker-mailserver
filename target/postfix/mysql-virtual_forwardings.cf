user = {{DB_USER}}
password = {{DB_PASS}}
hosts = {{DB_HOST}}
dbname = {{DB_NAME}}
query = SELECT destination FROM mail_forwarding WHERE source='%s' AND active = 'y' AND server_id = 1

#table = mail_forwarding
#select_field = destination
#where_field = source
# additional_conditions = and type != 'aliasdomain' and active = 'y' and server_id = 1
#additional_conditions = and active = 'y' and server_id = 1