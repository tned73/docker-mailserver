user = {{DB_USER}}
password = {{DB_PASS}}
hosts = {{DB_HOST}}
dbname = {{DB_NAME}}
query = SELECT domain FROM mail_domain WHERE domain='%s' AND active='y' AND server_id=1

#table = mail_domain
#select_field = domain
#where_field = domain
#additional_conditions = and active = 'y' and server_id = 1